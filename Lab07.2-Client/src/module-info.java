module com.entertaiment.client{
    //what other modules does this module depends on?
    //this module has access to the public classes of those exported packages only.
    requires com.entertainment; //this is a module name. modules depend on modules

}